    import XCTest
    @testable import remitx_forexJapan

    final class remitx_forexJapanTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(remitx_forexJapan().text, "Hello, World!")
        }
    }
